{
  "name": "timelimit-parent",
  "version": "0.1.0",
  "description": "Parent console for a TimeLimit sync server: protocol client, CLI, web console and LLM skill",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "timelimit-parent": "dist/cli/main.js",
    "tlp": "dist/cli/main.js"
  },
  "exports": {
    "./core": "./dist/core/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/web.mjs build",
    "typecheck": "tsc -p tsconfig.test.json --noEmit && tsc -p tsconfig.core.json && tsc -p tsconfig.web.json",
    "test": "rm -rf build && tsc -p tsconfig.test.json && node --test \"build/test/**/*.test.js\"",
    "test:e2e": "rm -rf build && tsc -p tsconfig.test.json && node --test --test-concurrency=1 build/test/e2e/server.e2e.js",
    "cli": "npm run build --silent && node dist/cli/main.js",
    "web": "node scripts/web.mjs dev",
    "web:mock": "node scripts/web.mjs dev --mock"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "esbuild": "^0.28.2",
    "typescript": "5.9"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "preact": "^10.29.8"
  }
}
